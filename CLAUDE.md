# Claude Code — start here

This project already has its working conventions and full status documented
elsewhere. Read both of these FULLY before doing anything else — do not
proceed until you have:

1. **AGENTS.md** (repo root or frontend/, wherever it currently lives) —
   the actual working conventions for this project: verification standards,
   what "done" means, protected files, Figma workflow notes. These apply
   to you exactly as they apply to any other agent working on this repo.
2. **docs/PROJECT_STATUS.md** — full architecture, what's built and
   verified, what's in progress, and critically: check for any
   "OUTSTANDING" section at the top. If one exists, resolve it before any
   new work, regardless of what task you were asked to do.

After reading both, confirm back with a short summary of current state
before starting work — do not just say "read, understood."

One Claude-Code-specific note: this repo is also being worked on by a
separate Gemini-based agent session in the same Antigravity environment.
Check `git log` and `git status` before starting — the other session may
have committed work since PROJECT_STATUS.md was last updated. If so, note
the discrepancy and ask before proceeding, rather than silently working
from a stale picture.